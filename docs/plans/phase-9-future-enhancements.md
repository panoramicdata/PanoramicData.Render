# Phase 9: Future Enhancements

**Depends on:** Phase 8 (Quality & Performance)

## Objective

Implement advanced native rendering features that are out of scope for the initial production release but are desirable long-term improvements.

## Steps

### 9.1 Native Chart Rendering

- [ ] 9.1.1 — Implement native chart rendering (bar, line, pie) using DrawingML chart parts (`c:chartSpace`)

### 9.2 Native SmartArt Rendering

- [ ] 9.2.1 — Implement native SmartArt layout using DrawingML diagram data

## Exit Criteria

- Native chart rendering produces faithful SVG/PDF output for common chart types (bar, line, pie)
- Native SmartArt layout produces faithful SVG/PDF output for common diagram types

## Notes

Items in this phase were originally tracked as "Future" items in earlier phase plans and moved here to keep those phases focused on production-scope work.
